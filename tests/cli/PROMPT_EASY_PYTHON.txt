Ensure you enable the Python 3 LSP before proceeding.

I would like you to implement the CLI in this repository. The readme has the information needed. 
It should be written in Python 3. The CLI should use the most popular idiomatic way of installing an application in Python, so someone can install the application in 
one line in the terminal, and also uninstall it and call the application globally from any directory.
The name of the Python application should match the repository name.

The CLI should ask for a password and an optional salt. 

It should allow --password but warn of the security risks of doing this (e.g. use an env var for this, wipe your terminal history entry when doing it)

Add a flag to the CLI to set the filename encoding (when it encrypts or decrypts the filename). This should be optional but should allow you to use 
base64 or base32 (or others - the default I believe is base32 with rclone)

Ensure the input file is specified with "-i" or "--input-file" and output file (this is optional) is specified with "-o" or "--output-file"

Write tests for the CLI to encrypt, decrypt files with and without a salt, using --password, and using custom encoding e.g. base64. 
If possible, also test the scenario where the CLI prompts for password/salt.

Ensure the README is up to date, following the format of rclone-web (keep the existing introduction text though), with bash examples.

Once complete, create a PR for your changes. Ensure the PR description is well formatted. Prompt the user to merge the PR.

Wait 5 minutes (in 1 minute increments), then try installing using the most popular way of installing standalone applications in Python and if that succeeds, try encrypting and decrypting
the files "kr9tu4e1da4u3nifdd99g9tf5o" and "Iyxcijgc9bp3o5Y0npW6xqUvwWNcc3MA4SadB0sR6cY" (base32 and base64 filenames - password is 'Testpassword1').
The filename should be TEST_FILE.txt and the contents should be a series of Bip39 style words. 

Uninstall the application via the Python application installer you used when done.
Tell the user how long the session took in minutes and seconds - from the initial prompt to successfully decrypting the two files.

Make sure you continue until the task is complete - which is the PR creation, and then waiting up to 5 minutes and downloading the application, then testing it.